<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fondo transparente (o blanco si prefieres) -->
    <solid android:color="#FFFFFF" /> <!-- Cambia a @android:color/transparent si quieres fondo transparente -->

    <!-- Bordes redondeados -->
    <corners android:radius="30dp" />

    <!-- Borde gris oscuro (#262626) -->
    <stroke
        android:width="1dp"
        android:color="#262626" />
</shape>